# Test Codebase: Flask API with Token Authentication

A Flask REST API for a task management system. API endpoints are protected using a `@require_api_token` decorator that validates bearer tokens from the `Authorization` header.
